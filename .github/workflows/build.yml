name: Build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Checkout AppliedEnhancements 1.0.9-fix
        uses: actions/checkout@v4
        with:
          repository: AyaYumi/AppliedEnhancements
          ref: 78bd11afc8db29573af067f2a28eb2cd7c403075
          path: .codex-tmp/appliedenhancements

      - name: Build separate prerequisite
        working-directory: .codex-tmp/appliedenhancements
        run: bash gradlew --no-configuration-cache build

      - name: Prepare compile dependency
        run: |
          mkdir -p libs
          cp .codex-tmp/appliedenhancements/build/libs/appliedenhancements-1.0.9-fix.jar libs/

      - name: Build with Gradle
        run: ./gradlew --no-configuration-cache build
