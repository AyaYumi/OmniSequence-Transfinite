# 万象构序: 超限 / OmniSequence: Transfinite

《万象构序: 超限》（OmniSequence: Transfinite）是面向 Minecraft 1.21.1 NeoForge 的 AE2 / ExtendedAE 后期附属 Mod，围绕分子重写、超限自动合成、量子链路与大型多方块构序系统展开。

项目技术 ID、资源命名空间与包名继续保持 `molecularmanipulator`，以兼容已有世界、配置与整合包脚本；从 `1.3.0` 开始，构建产物文件名改为 `omnisequence-transfinite-<版本号>.jar`。

## 核心特性

- 将 AE2 单次自动合成下单数量扩展到 `long` 范围，并提供可配置上限；超过 `Integer.MAX_VALUE` 的请求使用本模组的长整型下单链路。
- 可识别的无限物品来源会按 `Long.MAX_VALUE` 显示和参与网络数量汇总，避免继续显示为约 2.1G。
- 万物演算核心提供 `Long.MAX_VALUE` 级逻辑合成存储与并行能力，构序阵列多方块的批量构序并行上限同样提升到 `Long.MAX_VALUE`。
- 提供构序阵列与万物演算核心两套大型多方块，均支持有线 ME 连接、缠绕态量子链路和未成型施工联网。
- 所有自定义方块采用本模组独立的黑紫水晶材质；透明贴图保留 Alpha 通道并使用半透明渲染。

## 分子构序重写阵列

- 默认提供 720 个样板槽，界面每页显示 36 槽，共 20 页；配置可调整为 1～1000 页。
- 虚拟并行上限为 `2,147,483,647`，不会创建对应数量的线程或任务对象。
- 接收 AE2 合成任务后以最快 1 tick 完成一次配方处理。
- 产物通过持久化延迟缓冲返回 ME 网络，确保 AE2 合成 CPU 能正确识别中间产物。
- 支持 EAEP 通过 AE2 样板容器接口上传全部样板槽。

## 装配矩阵构序重写核心

- 可作为 ExtendedAE 装配矩阵内部功能方块，并替代普通合成核心与速度核心。
- 核心存在时，普通合成核心不再接收新任务，普通速度核心的速度值按 0 处理。
- 不创建普通合成核心的工作线程；任务在推送后进入下一 tick 的产物回传流程。
- 调用真实配方的 `assemble` 与 `getRemainingItems`，兼容容器返还、工具耐久和不消耗原物品的配方。
- 输出按 `AEKey` 聚合并批量返回 ME 网络，缓冲内容会随存档持久化。

## 万物演算核心

- 采用固定的 31×31×39 多方块结构，没有扩展层、升级槽或容量模块；结构完整成型后直接提供无限合成存储和无限并行任务通道。
- 一个物理控制器会按正在运行的合成请求动态创建 AE2 虚拟 CPU 通道，并始终保留一个空闲通道，因此新的合成请求不需要等待其他任务释放 CPU。
- 合成存储和内部逻辑并行上限均为 `Long.MAX_VALUE`；向 AE2 仍使用 `int` 的旧式并行接口报告 `Integer.MAX_VALUE` 哨兵值。以上数值只表示“没有模组设置的容量限制”，不会预分配对应规模的内存。
- 材料演算默认启用 `SAFE` 需求聚合：确定性的 AE2 处理样板与禁止替代的合成样板会被编译为有向无环图，重复子树合并后按拓扑顺序批量计算。模糊/NBT 替换、容器返还、多候选配方、自循环、动态输入和未知自定义样板会在子事务提交前自动回退 AE2 原始算法，不影响 ExtendedAE Plus 的最终计划重写。
- 任务提交后会立即补充空闲虚拟 CPU 通道。兼容批处理的供应器可以在一次推送中消化完整逻辑并行量；普通供应器按照每 tick 墙钟时间预算持续分配，避免用数十亿次逐项调用拖垮服务器。
- 万物演算核心不会把普通 AE2/ExtendedAE 样板供应器当作数十亿份材料的中间缓存：只有供应器或机器实际接收一次配方材料后，该次处理才从“计划合成”转入“正在合成”；尚未推送的原料留在合成 CPU 中，取消任务时由 AE 正常返还。供应器通过普通物品/流体容器投料时，多材料按剩余数量比例分轮发送，每轮所有材料都获得一次投料机会后才开始下一轮，避免第一种材料独占机器输入槽。产物始终必须由机器实际处理后返回，核心不会直接生成成品。
- 结构成型且联网时接管 AE2 材料演算：计算在受控数量的 AE2 后台线程中连续运行，不再受原版每 tick 微秒切片限制，也不会无限创建满载计算线程；相同网络材料快照、样板版本和请求条件下会复用进行中的计算或短期计划缓存。核心失效时自动回退到 AE2 原版计算路径。
- 材料演算会复用同一次下单捕获的 ME 库存快照，避免大型网络在创建缓存键和初始化 AE2 计算状态时重复扫描；常用配方输入的合法性与容器返还结果使用有界多项缓存，玩家手动下单在后台计算槽满时拥有一个独立交互槽。
- 结构损坏或区块暂时不完整时会保存主 CPU 与全部虚拟通道的任务、内部材料和进度；修复结构后恢复运行。
- 控制器尚未成型时可通过 ME 线缆或缠绕态量子链路进入“施工链路稳定”状态；此时仅允许从当前 AE 网络抽取一键搭建材料，不会开放合成存储、样板、合成、并行或材料演算能力，完整功能必须在结构成型后启用。
- 一键安放会先使用玩家背包，再从控制器当前连接的 ME 存储中抽取缺失材料；自动放置的结构部件会继承控制器朝向。
- 一键拆除每 tick 最多处理 128 个方块，保留控制器且只回收当前结构定义中预期的正确方块，不会破坏冲突方块。材料优先进入当前 AE 网络，其次进入玩家背包；两者均无空间时会安全停止。
- 万物演算控制器始终提供 15 级方块光，其他结构部件也会按类型发光；结构完整成型后，以结构中心为基准阻止周围 5×5 个区块内敌对生物的自然生成、区块生成、巡逻队生成和增援生成。该功能不会强制加载区块、删除已有生物或影响刷怪笼。
- 万物演算核心及其部件使用本模组独立的黑紫水晶模型和贴图，不再引用 AdvancedAE 资源；相关物品只在安装 AdvancedAE 后显示，但 AdvancedAE 不是本模组的强制前置。
- 新增方块：万物演算核心、演算核心框架、演算结构玻璃、无限并行矩阵、无限构序存储矩阵、全知演算矩阵、数据纠缠节点、演算能量稳定器、构序输出节点和演算晶体尖塔。

## 构序阵列多方块

- 控制器显示名为“构序阵列控制器”，其余部件统一使用“构序阵列”简称。
- 批量构序并行上限为 `Long.MAX_VALUE`；分子构序重写阵列和装配矩阵构序重写核心仍保持原有并行上限。
- 采用 31×46×31 的大型“星环构序仪”造型：控制器嵌入正面中央的平台外墙，底部收束为半径 4 格的紧凑底座，不再带有四条放射长边；主体包含倒锥晶体基座、双层环形宫殿、八座轨道尖塔、实体拱环、相位玻璃晶格球与顶部星冠。
- 完整结构需要 195 个外壳、156 个相位玻璃、52 个构序阵列量子水晶、453 个稳定器、1 个重写核心、2900 个 AE2 石英块、529 个 AE2 荧光石英玻璃、506 个 AE2 福鲁伊克斯块和 1 个控制器。
- 结构成型后显示发光能量球与动态星环；能量场、内核、主星环、副星环和核心晶格均可在控制器界面独立调整完整 RGB 颜色，合成时旋转会自动加速。
- 结构支持安全跨区块运行，不会主动强制加载区块；覆盖范围未完全加载时自动暂停，区块恢复后重新校验并继续工作。
- 支持真实结构校验、四向旋转、真实方块模型投影、一键安放和一键拆卸；投影按钮采用开关模式，已放置正确的方块会自动从投影中消失。
- 一键安放会先执行结构预检；如果现有结构只是缺少方块，则只生成缺失位置的定点修复队列，完整结构不会重复施工。
- 一键安放会识别并清理旧版方盒结构、旧 32×32 长通道结构和旧底部四向长边遗留的结构方块，便于原地改建。
- 控制器自身集成样板终端上传能力，结构中不再需要额外放置 EAEP 上传核心。
- 一键拆卸优先把回收的结构材料送入控制器连接的 ME 存储网络；网络不可用或空间不足时才回收到玩家背包。
- 阵列外壳、相位玻璃、构序阵列量子水晶、稳定器、核心和控制器均会主动发出不同等级的方块光，完整建筑在夜间无需额外照明。
- 完整成型的阵列会抑制阵列中心周围 5×5 个区块内敌对怪物的自然生成、区块生成、巡逻队生成和增援生成；不会强制加载区块，也不会删除现有怪物或影响刷怪笼。

## 缠绕态量子链路

- 控制器内置量子端点，并提供一个缠绕态奇点槽；整座构序阵列可替代量子网络桥的一端。
- 将一对缠绕态奇点的一半放入远端标准 AE2 量子环，另一半放入控制器，即可让控制器完整加入远端 AE 网络。
- 量子链路支持跨维度，而且不要求多方块预先成型；只放置控制器并装入配对的缠绕态奇点后，一键安放即可直接从远端 ME 网络抽取缺失材料。结构成型后的样板、合成任务、存储和能量，以及拆卸返还，也均通过远端网络工作。
- 远端只允许存在一座持有该频率的量子环；如果两个标准量子环已经完成配对，或该频率已经被另一台控制器占用，控制器会拒绝连接。
- 量子链路额外消耗 512 AE/t 和 1 个 AE 频道；远端区块卸载、量子环掉线、频道不足或网络缺电时断开，恢复后自动重连。
- 如果 AE2 因端点状态变化销毁了失效的虚拟连接，控制器会在条件恢复后自动重建链路，不需要重新放入缠绕态奇点。
- 无线量子模式不允许控制器同时接入另一套有线 ME 网络，避免意外合并两个独立网络。

## 放置与朝向

- 本模组注册的 18 个方块在玩家手动放置时都会让正面朝向玩家，避免控制器、功能部件和装饰部件默认背对放置者。
- 万物演算核心的一键搭建会让带方向属性的结构部件统一继承控制器朝向，保证自动搭建后的模型方向一致。

## 内部生产流水线

- 主产物和容器返还/副产物使用独立路由，可分别选择返回 ME 网络、保留在阵列内部或送往控制器指定方向的外部物品容器。
- 无论路由设置如何，阵列都会优先把 AE2 合成 CPU 正在等待的中间产物直接交付给对应任务。
- 内部产物进入持久化的高优先级构序缓存，后续合成任务会优先直接从缓存取料。
- ME 网络断开、存储空间不足或区块卸载时，尚未完成路由的产物不会丢失，恢复后会继续转运。
- 控制器界面显示缓存类型、缓存数量、待路由数量、堵塞状态和近期流水线吞吐量。

## 物质构序重写

- 控制器内置“分解标记 / 蓝图样品 / 重写产物”三个槽位，不需要额外放置功能核心。
- 支持金属、矿物、晶体和有机四类独立构序储量；首次启动会生成 `config/molecularmanipulator/matter_rewrite_rules.json`。
- JSON 中每个物品或物品标签都能分别配置 `deconstruct`（每件分解产出的序列）与 `rewrite`（每件复制消耗的序列）；缺少其中一项就会单独禁用对应操作，精确物品规则会覆盖标签规则。
- 命中 JSON 规则且不携带自定义数据的物品会在鼠标悬浮窗中显示“分解产出”和“复制消耗”，四类序列使用独立颜色，效果类似 EMC 数值提示。
- 默认 JSON 保留原有约 85% 的分解回收平衡，但配置数值就是实际产出/消耗量，可以按整合包需求完全修改；修改后重新进入服务器或单人世界即可加载。
- 解构和重写均消耗 ME 能量并积累重写熵值；熵值饱和时暂停工作，并以每秒 25 点的速度自然冷却。
- 携带附魔、命名、耐久、容器内容等自定义数据的物品不会被解构或复制，避免丢失数据或复制特殊物品。
- 左侧分解槽是非实物标记槽：用手中物品点击即可记录类型，不扣除手中物品；分解任务只从 ME 网络持续抽取匹配物品，缺货时保持开启并等待。
- 蓝图复制同样支持持续运行，但中央样品槽必须实际放入一个物品；蓝图样品不会被消耗，移除或更换样品会停止任务，序列不足时则保持开启等待。
- 分解和复制是两套可同时开启的独立任务，各自保存进度、累计数量、目标数量和停止原因；同一 tick 内分解新得到的序列可以立即供复制使用。
- 复制产物可在界面选择“输出 ME”或“仅输出槽”，不会在两种模式之间自动回退；所选目标填满后自动停止复制。
- 分解和复制可分别设定停止数量，`0` 表示无限；达到目标、构序仓满、原料耗尽、蓝图变化或输出存储满时会显示对应状态并安全停止，缺电、断网、序列不足和熵值过高时则暂停等待恢复。
- 控制器提供 4 个 AE2 加速卡槽；安装 0～4 张加速卡时，每件物品的处理周期依次为 20、10、5、2、1 tick。
- 当前任务类型、处理数量、周期进度、暂停/停止原因、数量目标和加速卡数量均会持久化并同步显示在控制器界面。
- 动态星环直接响应任务开关：仅分解时反向低速构序、仅复制时正向高速重写、两者同时开启时进入高强度双向共振；表现从点击开启时立即改变，不再由单件处理完成事件触发。

规则示例：

```json
{
  "rules": {
    "minecraft:iron_ingot": {
      "deconstruct": { "metal": 244, "mineral": 0, "crystal": 0, "organic": 0 },
      "rewrite": { "metal": 288, "mineral": 0, "crystal": 0, "organic": 0 }
    },
    "minecraft:nether_star": {
      "rewrite": { "metal": 0, "mineral": 4096, "crystal": 32768, "organic": 4096 }
    },
    "#c:ingots/*": {
      "deconstruct": { "metal": 244, "mineral": 0, "crystal": 0, "organic": 0 }
    }
  }
}
```

## 依赖

- Applied Energistics 2 `19.2.17` 或兼容版本
- ExtendedAE `1.21-2.2.32-neoforge` 或兼容版本
- Glodium `1.21-2.2-neoforge` 或兼容版本
- AdvancedAE `1.6.11` 或更高版本为可选依赖；未安装时模组仍可正常加载，但万物演算核心相关物品不会显示。
- ExtendedAE Plus 与 JEI 为可选兼容项，分别用于扩展样板交互和配方/结构展示。

## 配置

- `pattern_pages`：分子构序重写阵列样板页数，范围 1～1000，默认 20 页，每页 36 个槽位。
- `build_blocks_per_tick`：构序阵列一键安放/拆卸每 tick 处理方块数，范围 1～256，默认 32。
- `idle_power`：构序阵列控制器维持联网时的待机功耗，范围 1～4096 AE/t，默认 128 AE/t。
- `max_crafting_order_amount`：AE2 单次自动合成下单上限，范围 1～`Long.MAX_VALUE`，默认 `1,000,000,000,000`；极端数量仍可能受具体配方的中间计算限制。
- `omni_max_fast_mode`：万物演算核心安全需求聚合模式，默认 `SAFE`；设为 `OFF` 可完全使用 AE2 原始配方树计算。
- `omni_max_fast_max_nodes`：单次安全聚合最多编译的唯一配方节点数，范围 64～65536，默认 8192；超出后自动回退 AE2。
- `omni_max_fast_compile_budget_ms`：安全聚合图的最大编译时间，范围 1～5000 毫秒，默认 100 毫秒；超时后自动回退 AE2。
- `omni_max_fast_diagnostics`：是否记录聚合成功耗时、合并节点数与回退原因，默认关闭。
- `omni_batch_dispatch_enabled`：是否允许兼容供应器一次抽取并推送多次配方材料，默认开启；关闭后执行阶段全部回退 AE2 单次推送。
- `omni_batch_allow_substitution_patterns`：是否允许开启物品替换的样板进入批量推送，默认关闭；仅开启流体替换的样板仍属于确定性输入，默认允许使用聚合计算和批量推送。容器返还和可重复使用输入始终回退单次推送。
- `dynamic_effect_level`：客户端动态多方块特效等级，`0` 为关闭、`1` 为精简、`2` 为完整天体仪与量子星门，不影响合成速度。

## 模型与贴图

- 主机器模型：`src/main/resources/assets/molecularmanipulator/models/block/molecular_manipulator.json`
- 装配矩阵核心模型：`src/main/resources/assets/molecularmanipulator/models/block/assembler_matrix_molecular_core.json`
- 方块模型目录：`src/main/resources/assets/molecularmanipulator/models/block/`
- 方块贴图目录：`src/main/resources/assets/molecularmanipulator/textures/block/`
- 当前资源统一采用黑紫水晶风格；玻璃、能量层与其他半透明贴图保留透明通道，并通过 `translucent` 渲染层显示。
- 正式模组名为“万象构序: 超限 / OmniSequence: Transfinite”，技术资源路径和注册 ID 继续保持 `molecularmanipulator`。

## 构建

```powershell
./gradlew.bat clean build --no-configuration-cache
```

当前版本为 `1.3.2`，产物为 `build/libs/omnisequence-transfinite-1.3.2.jar`。
