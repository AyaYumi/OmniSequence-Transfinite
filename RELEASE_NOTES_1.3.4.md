# OmniSequence: Transfinite 1.3.4 更新说明

适用版本：Minecraft 1.21.1 / NeoForge
本说明汇总自 1.3.2 之后的 1.3.3 与 1.3.4 更新。

## 主要更新

- 重构万物演算核心的实际材料发配：
  - 普通 AE 合成供应器会按照 `1 → 2 → 4 → 8 → …` 动态扩大完整配方批次。
  - 只要整批材料成功输入，就会继续扩大下一批，不再等待产物返回。
  - 机器拒收或拥堵后会保留最后一次成功倍率，并在后续重新探测。
  - 明确支持批处理的机器继续使用 `long` 逻辑批量直推。
- 增强第三方样板供应器兼容：
  - 保留 ExtendedAE Plus 缩放样板包装信息。
  - 保留 AdvancedAE 定向输入信息。
  - 保留 AE2LT 过载样板供应器元数据。
  - 同一样板存在多个供应器时采用公平选择，避免满载或拒收的目标长期阻塞其他目标。
- 为不接受倍增样板的机器增加同 Tick 单份高速回退：
  - 成功接收 `1×`、但拒绝 `2×` 的供应器，会在当前订单内固定使用完整单份样板。
  - 每次仍由 AE2 重新抽取一整份配方并独立记账，不会拆散多材料样板。
  - 多个 CPU 通道共享安全额度，需求通道会优先获得空闲额度。
  - 单份额度耗尽后只暂停单份路径，倍增样板和显式 `long` 批量仍可继续。

## 修复内容

- 修复万物演算 CPU 在确认合成计划时，缺失材料列表每次变化的问题。
- 修复 AE2: Crafting Tree 等扩展数据在确认数据包中丢失的问题。
- 恢复 AE2 原生库存快照与暂停协作流程，避免计算期间供应器状态变化造成竞态。
- 修复慢速、高并行、大输入缓存机器必须等待首轮产物后才继续发料的问题。
- 修复无产物“假合成”无法继续发配的问题；现在以输入成功作为发配反馈。
- 修复多材料配方中第一种材料长期占用输入槽、其他材料无法进入的问题。
- 修复供应器排队状态在区块重载或游戏重启后丢失的问题。
- 修复批量任务的 AE2 `waitingFor` 累计越过 `Long.MAX_VALUE` 后变成负数并卡单的问题。
- 修复 AE2WTLib 自动补货数量溢出后，快捷栏数量格式化崩溃的问题；无限数量显示为 `∞`。
- 修复万物演算核心渲染时出现 `BufferBuilder: Not building!` 的客户端崩溃。
- 修复 EAP 虚拟合成最终批次可能残留幽灵任务或幽灵产物的问题。

## 配置变化

- 新增 `omni_unscaled_dispatch_attempts_per_tick`
  - 默认值：`32`
  - 范围：`1～256`
  - 含义：每个万物演算核心、每 Tick、所有 CPU 通道共享的真实单份样板调用上限。
  - 提高数值可以加快不支持倍增的机器，但可能拉长服务器 Tick。
- `omni_dispatch_max_work_units` 改为 `long`
  - 默认值：`2147483647`
  - 最大值：`Long.MAX_VALUE`
- 删除以下失效选项：
  - `omni_provider_max_queued_items`
  - `omni_provider_send_operations`
- 配置文件中若存在当前版本已经删除的旧选项，启动时会保留最多五份 `.toml.bak`，再按最新默认配置重新生成。仅缺少新选项或数值越界时只会定向补齐或修正。

## 兼容性与升级

- 现有世界、样板和方块数据保持兼容。
- Mod ID 仍为 `molecularmanipulator`。
- 客户端与服务端需要安装相同版本。
- 升级前建议备份世界与配置，并删除旧版 JAR，避免同时加载多个版本。
- 如果配置被自动重建，可根据生成的 `.toml.bak` 重新应用自定义数值。
- 本构建仅适用于 Minecraft 1.21.1 NeoForge，请勿放入 Forge 1.20.1 环境。
