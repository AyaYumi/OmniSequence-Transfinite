# 万象构序：超限 1.3.5-fix 修复说明

发布日期：2026-07-28
适用环境：Minecraft 1.21.1 / NeoForge / Applied Energistics 2 19.2.17+

## 本次修复的核心问题

此前，万象演算在处理“一张样板包含多种原料、一个样板供应器连接多台机器”的配方时，会尝试把样板整体倍增。部分普通样板供应器会按原料类型分开发送，导致第一批机器被第一种原料塞满、后续机器被第二种或第三种原料塞满，最终没有任何一台机器拿到完整配方，合成因此卡死。

`1.3.5-fix` 对普通多原料供应器改为重复发送完整的原始 `1x` 配方。每次调用都包含该配方所需的全部原料，只有完整的一份配方被目标接受后才继续下一份，避免原料被拆散到不同机器。

## 发配与性能规则

- 明确实现批量接口的端点仍可一次接收聚合后的超大批次。
- 安全的单原料样板继续使用跨刻自适应倍增，按 `1 → 2 → 4 → 8 ...` 提升吞吐量。
- 普通多原料样板只重复发送完整 `1x` 配方，不再把运行时倍增样板直接交给未知供应器。
- `2,147,483,647` 仍是逻辑并行上限，不代表主线程会在一个游戏刻内执行 21 亿次 Java 调用。
- 所有普通兼容调用共享服务器级、负载自适应的时间预算；服务器负载升高时预算会自动收缩，并在任务和样板之间轮转，避免长时间卡住主线程。
- 对同一任务、样板和游戏刻缓存“不支持显式批量接口”的检查结果，减少百万级请求中重复拓扑扫描和临时对象分配。

## 为什么不能完全照搬 Neo ECO 的千万级速度

Neo ECO 的百万/千万级快速路径建立在它自己的 Pattern Bus 与 Worker 闭环上：控制器把“样板 + 次数”作为一个聚合任务交给 Worker，Worker 在内部完成计数和执行，因此不需要对普通 AE2 样板供应器调用数百万次。

普通 AE2、ExtendedAE 或第三方样板供应器只暴露“尝试推送一次样板”的兼容接口。对于未知的一对多目标，若把多原料配方直接倍增，无法证明所有原料会进入同一台机器；若逐份调用，则实际成本必然随配方次数增长。本版本采用的策略是：已知批量端点走聚合快路，安全单原料走倍增快路，未知多原料走完整配方兼容路径并受时间预算保护。

## 其他修复

- 修复 ExtendedAE Plus 在计划阶段已经放大的多原料样板可能绕过完整配方保护的问题。
- 修复 Mixin 辅助类被目标类加载器非法加载而导致的启动崩溃。
- 修复打开超长合成数量输入界面时出现 `NoClassDefFoundError: NumberEntryWidgetAccessor` 的问题。
- 删除已经失效的“旧版万物演算单份推送上限”配置项。
- 旧配置文件会只移除废弃键，保留其他自定义数值和注释。

## 安装与升级

1. 完全退出客户端和服务端。
2. 在 `mods` 目录中只保留 `omnisequence-transfinite-1.3.5-fix.jar`。
3. 不要让 `omnisequence-transfinite-1.3.5.jar` 与修复版同时存在，否则会产生重复 Mod。
4. 重新启动游戏并复测原先会发生多原料分流的样板。

构建产物 SHA-256：`084DB7DDA828C4D1C3722992996DAC4B28B659DE8DC1C56C3CB3A09A9D33F75E`
