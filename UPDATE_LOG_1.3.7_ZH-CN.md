# 万象构序：超限 1.3.7 更新日志

发布日期：2026-07-30

## 目标版本

- `omnisequence-transfinite-1.3.7.jar`
  - Minecraft 1.21.1
  - NeoForge 21.1.230+
  - Applied Energistics 2 19.2.17+

## 多方块结构更新

- 两个固定多方块都增加了新版布局，实体方块不会再占用大型特效的中心位置。
- 构序阵列会让本地坐标 `(0, 29, 0)` 保持为空气，并将实体核心移动到顶部锚点
  `(0, 36, 0)`。
- 万物演算核心会让本地坐标 `(0, 17, 0)` 保持为空气，并将数据纠缠节点移动到
  前方下部塔体 `(0, 9, -12)`。
- 完整旧版布局仍然有效，可以正常成型并保留全部功能。
- 控制器检测到旧版布局时会显示可选的结构更新提示，是否迁移由玩家决定，
  不强制重新搭建。
- 结构投影、自动搭建、自动拆除、JEI 预览、材料清单和 GuideME 文档均会使用
  正确的新旧布局。

## 搭建与兼容性修复

- 构序阵列在生存模式下自动搭建时，会正确使用已连接 ME 网络中的材料。
- 创造模式搭建不再等待材料，放置失败时也不会生成返还方块。
- 之前支持的搭建方式和完整旧版结构继续兼容。

## 自动合成、可复用输入与取消

- 物品替代样板仍会从 MAX_FAST 规划中回退，但永久允许进入兼容的运行时批量发配；
  实际输入由 AE2 选择。已停用的 `omni_batch_allow_substitution_patterns`
  会从现有 TOML 中移除，其他自定义值不会被重置。
- 装配矩阵构序重写核心与构序阵列会跨存档、区块卸载和服务器重启持久化所有已接受的
  可复用输入批次。
- 取消 AE2 合成任务会持久停止供应器侧全部剩余执行，并精确退回未使用材料及可复用
  物品的当前状态；已完成的产物仍有效，重载后也不会恢复已取消任务。
- 同键返还和不可损坏物品可作为一个可复用批次执行。有限耐久工具仅在每次合成都确定
  增加恰好 1 点损伤时批量处理；耐久附魔、随机变化、依赖上下文及换键返还会继续走
  AE2 原生逐份路径。
- 批量扩展会对实际合并后的输入调用 AE2 原生样板能耗计算，保持原版合成能耗语义。

## 界面与文档

- GuideME 文档现在独立显示在“万象构序：超限”顶级分类下，同时保留物品页面跳转。
- 控制器界面在响应式缩放时会保留原版黑色半透明世界背景，并覆盖整个屏幕。
- 控制器界面会在空间不足时自动缩小，槽位、按钮、提示框和 JEI 幽灵材料会使用
  相同的变换坐标。
- 物质构序详情默认显示紧凑的按住 Shift 展开提示；客户端配置支持 `DISABLED`、
  `HOLD_SHIFT` 和 `ALWAYS_VISIBLE`。
- 样板库存支持带防抖的跨页输入/输出搜索，并保留 AE2 编码样板的完整悬停详情；
  “通用拼音搜索”仍为可选模组，安装后可直接使用拼音匹配样板。
- 英文标签和数值会按实际字体宽度排列，修复标题、页码、序列数量、量子状态等
  文本相互覆盖的问题。
- 两条物质作业状态与计数会分别适配各自 96 像素宽的栏位。
- 万物演算核心的固定能力说明改为正常字号自动换行。
- AE2 较窄的合成 CPU 列表会显示 `Omni Core · L1` 等紧凑名称，鼠标悬停时仍会
  显示完整通道名称。
- 英文和中文翻译键已经逐项检查并保持完整对应。

## 投影性能优化

- 方块投影改为复用 16x16x16 分区顶点缓冲，不再每帧重新绘制全部投影方块。
- 发生变化的分区会渐进重建，每帧最多处理两个；未变化的几何会直接复用。

## 安装与旧存档

1. 完全关闭客户端和服务端，并备份世界。
2. 删除或停用旧版 `omnisequence-transfinite-*.jar`。
3. 只安装与 Minecraft 版本及加载器对应的 JAR。
4. 每个 `mods` 目录中只保留一个启用中的万象构序 JAR。
5. 现有完整多方块可以原样继续运行；只有希望迁移到清空特效中心的新布局时，
   才需要在控制器中确认结构更新。

## SHA-256

- `omnisequence-transfinite-1.3.7.jar`
  `22ECA3C8A925DC6D43E4BC2D18F5A4A1F2027B1DC12974D2E505F1899E987304`
